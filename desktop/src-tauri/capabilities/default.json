{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities: command invocation, live event streams, dialog and notifications for the AetherAV window.",
  "windows": ["main", "widget"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "dialog:default",
    "notification:default"
  ]
}
